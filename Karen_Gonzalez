def is_palindrome(palindrome):
    
    palindrome = palindrome.replace(" "," ")
    l = len(is_palindrome)
    x = 0

    for i in reversed(range(0,l)):
        if palindrome[i].lower() != palindrome[x].lower():
            return False
        x += 1
    return True
